--- src/commands/conflicts.ts
+++ src/commands/conflicts.ts
@@ -15,7 +15,7 @@
 import { getStorage } from '../storage/index.js';
 import chalk from 'chalk';
 import { getConflictEngine } from '../lib/conflict-engine.js';
-import { validateAgentId, validateBranchName } from '../lib/security.js';
+import { validateBranchName } from '../lib/security.js';
 import { isExcludedPath } from './register.js';
 import { loadConfig } from '../lib/config.js';
 import { logActivity } from '../lib/activity.js';
@@ -139,8 +139,8 @@
             for (const file of conflict.files) {
                 console.log(chalk.gray(`      - ${file}`));
             }
-            const fileList = conflict.files.slice(0, 3).map(f => chalk.gray(f)).join('\n      - ');
-            const more = conflict.files.length > 3 ? chalk.gray(`\n      ... and ${conflict.files.length - 3} more`) : '';
+            // const fileList = conflict.files.slice(0, 3).map(f => chalk.gray(f)).join('\n      - ');
+            // const more = conflict.files.length > 3 ? chalk.gray(`\n      ... and ${conflict.files.length - 3} more`) : '';

             if (conflict.diffs) {
                 console.log(chalk.yellow('\n      Detailed Diff Overlap:'));
