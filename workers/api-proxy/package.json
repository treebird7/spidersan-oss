{
  "name": "spidersan-api-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker — api.spidersan.dev proxy to fine-tuned model backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.84.0"
  }
}
