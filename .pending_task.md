# Pending from May 11, 2026 (21:35 — treesan session)

## P0 — Must Do Next
- [ ] Restart smalltoak.py server with new `SMALLTOAK_TOKEN` — client side rotated to vault (`spidersan/SMALLTOAK_TOKEN v1`), server needs restart: `envoak vault inject --service spidersan -- python3 ~/Dev/smalltoak/smalltoak.py`
- [ ] `cd ~/Dev/spidersan && pnpm publish` — publish CLI v0.10.0
- [ ] `cd ~/Dev/spidersan/mcp-server && npm publish --access public` — publish spidersan-mcp v1.1.0

## P1 — This Week
- [ ] Deprecate old npm versions (after publish):
  - `npm deprecate 'spidersan@<0.9.0' 'Outdated — please upgrade to 0.10.0 or later.'`
  - `npm deprecate 'spidersan-mcp@<1.1.0' 'Outdated — please upgrade to 1.1.0 or later.'`
- [ ] Update spidersan SKILL.md: add `watch --fetch-poll` to Quick Reference table
- [ ] Update CONTEXT.md: add renderer libs to module map (pulse-renderer, watch-renderer, register-renderer)

## P2 — Future
- [ ] `validateRemoteName` for `remote` param in `src/lib/remote-drift.ts` — ℹ️ finding from ts-review; branch is validated, remote is not (no shell so not critical, just inconsistent)
- [ ] ts-review remaining commands (watch.ts, conflicts.ts, sync.ts)
- [ ] DEEPENING-11 candidates: graph-module integration, cross-machine pulse
- [ ] MCP list_tools count test — update if more tools added (currently 28)

## Key Context
- v0.10.0 pushed (18df02d), NOT yet on npm
- SMALLTOAK_TOKEN rotated — new value in vault at spidersan/SMALLTOAK_TOKEN v1; server still running old token until restart
- ts-review session landed security fixes to bot.ts, pulse.ts, remote-drift.ts (5 ⚠️ fixes, 161/161 tests)
- ts-tree pattern promotions: env-isolation (two-tier helper), error-discipline (named-intent catch), spawn-hygiene (execFileSync timeout)
- Gold pairs: 2 added to spidersan-ai (pool: 1552), Yosef notified
- ts-review curation gate: pattern promotions must be user-approved before writing — do not auto-append
- docs/arch/ (STATE.json + contracts/) is gitignored — files on disk only

## Files to Check
- `src/commands/bot.ts` — env isolation fix (gitEnv/spidersanEnv helpers added)
- `src/lib/remote-drift.ts` — GIT_TIMEOUT_MS + isOfflineError now exported
- `src/commands/watch.ts` — fetch-poll loop (startFetchPollLoop export)
- `mcp-server/src/server.ts` — pulse_remote_drift tool ~line 271, list_tools count = 28
