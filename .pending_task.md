# Pending from March 3, 2026 (evening close)

## P0 — Must Do Tomorrow
- [ ] Re-provision `~/.envoak/ssan.key` (expired) — Toak messaging offline, ping bsan
- [ ] Port cleanup N+1 fix to private spidersan `src/storage/supabase.ts`

## P1 — This Week
- [ ] Wire `logActivity()` into `sync` and `conflicts` commands (register/merged/abandon done)
- [ ] nanoclaw ToakBox — blocked on sherlocksan security review + treesan blessing
- [ ] spidersan-oss: pull 6 commits from remote (behind)
- [ ] nanoclaw-private: 4 commits behind remote — pull

## P2 — Future
- [ ] GitHub App (L3 of activity-log spec) — webhook push-based auto-registration
- [ ] `spidersan --escalate` flag: ping control tower on TIER 2+ conflicts
- [ ] spidersan-github-dashboard F6: FlockView web integration
- [ ] Watsan BM25 `--bm25-only` flag

## Completed This Session ✅
- [x] `spidersan log` — L1 Activity Log command (wired into register/merged/abandon)
- [x] `spidersan daily` — L2 Daily Bridge (smart table parser, agent colors, context mode)
- [x] /spidersan skill updated with log/daily/doctor --remote in sync-all workflow
- [x] Daily collab updated, skills repo + treebird-internal pushed

## Key Context
- ssan.key at `~/.envoak/ssan.key` is EXPIRED — collab writes done directly to file
- Activity log JSONL at `~/.spidersan/activity.jsonl` (local) + Supabase flush on sync
- Daily bridge reads `$TREEBIRD_INTERNAL/collab/daily/YYYY-MM-DD-daily.md`
- Parser uses `looksLikeTimeOrAgent()` heuristic — no fixed header required

## Files to Check
- `src/commands/sync.ts` — add logActivity('sync') call
- `src/commands/conflicts.ts` — add logActivity('conflict_detected') call
- `src/lib/activity.ts` — Supabase flush in queryActivityLog, local JSONL primary
