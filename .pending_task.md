# Pending from March 24, 2026 (21:40)

## P1 — This Week
- [ ] `npm publish` v0.5.0 — dist is built, lockfile updated, run from spidersan-oss
- [ ] Reply to ChatGPT test ping — Toak inbox, `chatgpt:spidersan` channel
- [ ] `registry-sync --push` — needs `spider_registries` table + RLS in colony Supabase project (dknahxavnrtaqlatflot). Auth fails with anon key — check RLS policies or use service role key.
- [ ] `spidersan.treebird.uk` command reference page — waiting on artisan to update

## P2 — Future
- [ ] Treesan D1 `spidersan.rs` Rust bridge — blocked on treesan
- [ ] `spidersan/messages` branch — experimental git-based messaging, 8 weeks old. Merge or delete?

## Key Context
- 0.5.0 tagged + pushed (bf4d327 is HEAD, v0.5.0 tag on db6992a)
- All execSync removed — execFileSync only policy now enforced
- socket.io-parser 4.2.6 (was 4.2.5 CVSS 8.7)
- Jules/Codex keep re-filing execSync PRs — pattern: fix directly + close with `gh pr close <N> --comment "fixed in main"`
- colony status JSON key is `signals`, not `gradient` (queen.ts already fixed)
- COLONY_SUPABASE_URL/KEY are the vault-injected vars (not SUPABASE_URL/KEY)

## Files to Check
- `src/commands/registry-sync.ts` — DB auth fix pending
- `CHANGELOG.md` — [Unreleased] section ready for next cycle
