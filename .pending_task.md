# Pending from March 12, 2026

## P0 — Must Do Next Session
- [ ] `ssan.key` renewal — ssan key is revoked, memoak inject fails. Ping bsan to re-provision
- [ ] `cp-cli` key expired 2026-03-10 — needs new grant

## P1 — This Week
- [ ] PWA colony close flow — mobile-initiated session close (emit signal + ack broadcast from PWA)
- [ ] Heartwood tsan section — fill section in `treebird-internal/collab/COLLAB_heartwood-fleet-design_2026-02-20.md` (broadcast `7700d5d3` pending since Mar 4)
- [ ] `spidersan stale --auto-cleanup` flag (spec gap logged in Mode 1 spec)

## P2 — Future
- [ ] Rust port of Mode 1 treetop observer (trigger conditions in spec)
- [ ] `spidersan conflicts --ecosystem` multi-repo primitive
- [ ] PWA push notifications — service worker `push` event for colony broadcasts

## Completed This Session ✅
- [x] Sync: committed vault provider test Tauri bridge (d4dbceb)
- [x] Sync: committed vault keys revoke label/prefix resolution (a1450a0)
- [x] Sync: committed PWA useOnline + usePullToRefresh hooks (02e6eaa)
- [x] Build `spidersan watch treetop` — Mode 1 treetop observer
  - --dry-run runs one full tick cleanly (T3:14, T2:0 detected in Toak)
  - Adaptive interval: 15s/30s/120s/300s based on tier counts
  - TIER 3 broadcast deduplication (once per unique conflict pair per session)
  - Colony GC + stale check per tick

## Key Context
- Mode 1 spec: `treebird-internal/waltsan/realism/SPEC_spidersan_mode1_treetop_observer.md`
- Entry: `spidersan watch treetop [--dry-run] [--repos <paths>] [--interval <secs>]`
- Treesan D1: `spidersan.rs` Rust bridge — waiting on treesan
