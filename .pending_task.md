# Pending from March 3, 2026

## P0 — Must Do Tomorrow
- [ ] Port cleanup N+1 fix (spidersan-oss #42) to private spidersan — `src/storage/supabase.ts` still has old code
- [ ] Re-provision `~/.envoak/ssan.key` (expired) — Toak messaging is offline, ping bsan

## P1 — This Week
- [ ] Wire `activity.ts` into CLI commands (register, merge, abandon) — written but not yet wired
- [ ] `doctor` remote health feature — finish + wire into `doctor` command
- [ ] Activity Log spec dispatched by treesan (`ssan-activity-log-001`) — L1 Supabase migration + `spidersan log` command
- [ ] Daily Bridge spec dispatched by treesan (`ssan-daily-bridge-001`, depends on L1) — `spidersan daily` command + collab reader
- [ ] Watsan BM25 — wired into hybrid search, may want `--bm25-only` flag
- [ ] nanoclaw ToakBox branch — blocked on sherlocksan security review + treesan blessing

## P2 — Future
- [ ] GitHub App (L3 of activity-log spec) — webhook receiver for push-based auto-registration
- [ ] spidersan-github-dashboard F6: FlockView web integration
- [ ] Check private spidersan repo MCP config
- [ ] `spidersan --escalate` flag: ping control tower on TIER 2+ conflicts

## Key Context
- Systemwide branch tidy completed Feb 25 — all 18 repos cleaned
- Activity Log + Daily Bridge spec approved by treesan, dispatched as invoak tasks
- Watsan BM25 hybrid search live (70% vector + 30% keyword via RRF)
- Artisan copilot worktree cherry-picked (safe files only, server.js excluded — stale fork, not malicious)
- Myceliumail pool command wired, desktop launcher fixed (graceful shutdown)
- Toak: 3 branches merged (implement-toak-4, perf-invoak-async-io, feat/toak-supabase-realtime)

## Files to Check
- `src/lib/activity.ts` — needs wiring into commands
- `src/commands/doctor.ts` — remote health additions
- `collab/specs/SPEC_spidersan_activity_log_daily_bridge.md` — approved spec, in dispatch queue
- `/Users/freedbird/Dev/Watsan/src/commands/search.ts` — BM25 integration live
- `/Users/freedbird/Dev/Watsan/src/rag/storage.ts` — fetchAllChunks() added
