# Pending from April 18, 2026 (19:15)

## P0 — Must Do Next

- [ ] Phase C: wire `handleEvent()` into git-watch — see `phase-c-context.md` in session files for full plan
  - Hook: `src/lib/git-events-subscriber.ts` inside `handlePush()` enqueue() callback
  - Check `src/commands/bot.ts` first — m5 may already have drain logic
  - Map GitEvent → EventPayload (no files initially, TIER 1 fallback)
  - Test: `spidersan git-watch --once` should emit TIER 1 advice

## P0 — Must Do Tomorrow (carried)
- [ ] Merge `sherlock/vault-session-refactor-pp1` → main — unblocks all npm-installed agents on `envoak identity push`
- [ ] Confirm memoak embeddings live once LM_STUDIO_URL wired in Supabase (coordinate with watsan)

## P1 — This Week
- [ ] Run `envoak identity push` on spidersan CLAUDE.md once sherlock merged to main
- [ ] Close Envoak #26 on GitHub (memoak v2 is live, verify_jwt=false confirmed working)

## Key Context
- memoak edge fn is `verify_jwt=false` — X-Agent-Token (64-char hex) is sole auth, no JWT required
- envoak globally installed from `sherlock/vault-session-refactor-pp1` via pnpm on m2
- pnpm home: `/Users/freedbird/Library/pnpm` (added to ~/.zshrc)
- ingest docs: `treebird/knowledge/cookbooks/COOKBOOK_memoak_ingest.md` + `treebird-internal/supabase/functions/memoak/README.md`

## Files to Check
- `~/Dev/envoak/src/commands/identity.ts` — ingestToMemoak auth chain (verify_jwt=false path, sherlock branch)
- `~/Dev/treebird-internal/supabase/functions/memoak/README.md` — updated this session
- `~/Dev/treebird/knowledge/cookbooks/COOKBOOK_memoak_ingest.md` — new this session

## P1 — This Week
- [ ] Run yosearch to mine gitops edge cases → expand gold set
- [ ] Improve CLI UX: queen vs torrent naming, explain discoverability, advise inline behavior
- [ ] Investigate memoak v2 migration (Envoak #26)

## P2 — Future
- [x] Phase 4: GitHub App + webhook (spidersan-bot) — e2e working 2026-04-26
- [ ] Phase 5: Persistent agent mode

## Key Context
- AI playbook is PHC-optimized at 0.972 (18 scenarios in reasoner.ts, commit cb1b367)
- CLAUDE.md has the same playbook (gitignored, local identity)
- Vault label is "spidersan" (full name per canonization)
- Remaining weak spots (proactive_advise 0.63, ai_context_analysis 0.70) are Gemma variance, not playbook gaps

## Files to Check
- src/lib/ai/reasoner.ts — SCENARIO_PLAYBOOK (deployed, don't touch without re-scoring)
- CLAUDE.md — local identity with playbook section
- ~/Dev/promptclimb/scorer_spidersan.py — custom scorer (SPIDERSAN_LLM_URL must be base URL only)
- ~/Dev/promptclimb/prompt_sonnet_spidersan.md — Sonnet proposer instructions (source of truth for playbook)
